#alias git=hub
alias reload='. ~/.bash_profile'

alias '..'='cd ..'

alias clone='cdgit && git clone $1'

# General
alias l='ls -lah'
alias c='clear'
alias o='open .'
alias e='subl'
alias tf='tail -f -n 100'
alias httpdump='sudo tcpdump -i en0 -n -s 0 -w - | grep -a -o -E "Host\: .*|GET \/.*"'

# GIT
alias cdgit='cd /Users/shayneburgess/git'
alias gb='git branch'
alias gl='git log'
alias glo='git log --oneline'
alias gc='git commit -v'
alias gca='git commit -v -a'
alias gcam='git commit --amend'
alias gd='git diff'
alias gp='git pull'
alias gpr='git pull --rebase'
alias gpu='git push'
alias gst='git status'
alias gss='git stash save'
alias gsa='git stash apply'
alias gsp='git stash pop'
alias gsl='git stash list'
alias grc='git rebase --continue'
alias gri='git rebase --interactive'
alias gpuh='git push heroku master'

alias kc='kubectl'
alias kcgp='kubectl get pods'
alias kcgs='kubectl get services'
alias kcgd='kubectl get deployments'

alias cdgo="cd /Users/shayneburgess/git/go/src"
export GOPATH=$HOME/git/go # don't forget to change your path correctly!
export GOROOT=/usr/local/opt/go/libexec
export GOBIN=$GOPATH/bin
export PATH=$PATH:$GOPATH/bin
export PATH=$PATH:$GOROOT/bin
export PATH=/usr/local/bin/:$PATH
